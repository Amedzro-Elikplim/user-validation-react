import React from 'react';

export default class Logo extends React.Component{
    render(){
        return(
            <div className="stock-tracker-logo">
                <h1>STOCK TRACKER</h1>
            </div>
        )
    }
}